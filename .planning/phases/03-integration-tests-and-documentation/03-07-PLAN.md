---
phase: 03-integration-tests-and-documentation
plan: 07
type: execute
wave: 2
depends_on: ["03-06"]
files_modified:
  - trading/src/contract.rs
  - trading/src/interface.rs
  - trading/src/types.rs
  - trading/src/trading/rates.rs
  - trading/src/trading/market.rs
  - trading/src/trading/position.rs
  - trading/src/trading/adl.rs
  - trading/src/trading/execute.rs
  - trading/src/trading/actions.rs
  - trading/src/trading/config.rs
  - trading/src/validation.rs
  - trading/src/errors.rs
  - trading/src/constants.rs
  - trading/src/storage.rs
  - trading/src/events.rs
  - strategy-vault/src/contract.rs
  - factory/src/lib.rs
  - price-verifier/src/lib.rs
  - price-verifier/src/pyth.rs
  - governance/src/lib.rs
  - treasury/src/lib.rs
autonomous: true
requirements: [DOC-03, DOC-04]

must_haves:
  truths:
    - "Every public entry point function in all 6 contracts has a rustdoc comment with description, parameter semantics (units), and panics/errors"
    - "Critical internal math functions (rates, settlement, ADL) have rustdoc with formula documentation"
    - "Non-obvious code has inline WHY annotations explaining design decisions"
  artifacts:
    - path: "trading/src/contract.rs"
      provides: "Rustdoc comments on all Trading trait impl methods"
      contains: "/// "
    - path: "trading/src/trading/rates.rs"
      provides: "WHY annotations on funding and borrowing formulas"
      contains: "WHY:"
    - path: "trading/src/trading/market.rs"
      provides: "Rustdoc on Market methods with parameter units and rounding rationale"
      contains: "/// "
  key_links:
    - from: "trading/src/interface.rs"
      to: "docs/audit/PROTOCOL-SPEC.md"
      via: "Rustdoc references protocol spec sections"
      pattern: "Protocol Spec|PROTOCOL-SPEC"
---

<objective>
Add audit-focused rustdoc comments and inline decision annotations across all contracts.

Purpose: DOC-03 requires every public function to have rustdoc. DOC-04 requires inline decision annotations on non-obvious code. This helps auditors understand intent without guessing.

Output: Comprehensive rustdoc comments across all 6 contracts and inline WHY annotations.

Review feedback addressed:
- HIGH: Scope prioritized by audit value -- public entry points first, then critical internal math, then types/config, then only storage/events/constants if budget remains. NOT a blanket comment sweep.
- MEDIUM: Trivial getters/setters get one-line descriptions only (skip verbose format per D-12)
- MEDIUM: WHY annotations only where intent is not obvious from code or rustdoc (not everywhere)
- MEDIUM: Split into two tasks: trading crate (largest, most critical) and all other contracts. If budget runs low, trading is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-integration-tests-and-documentation/03-CONTEXT.md
@.planning/phases/03-integration-tests-and-documentation/03-06-SUMMARY.md

@docs/audit/PROTOCOL-SPEC.md
@trading/src/contract.rs
@trading/src/interface.rs
@trading/src/types.rs
@trading/src/trading/rates.rs
@trading/src/trading/market.rs
@trading/src/trading/position.rs
@trading/src/trading/adl.rs
@trading/src/trading/execute.rs
@trading/src/trading/actions.rs
@trading/src/trading/config.rs
@trading/src/validation.rs
@trading/src/errors.rs
@trading/src/constants.rs
@trading/src/storage.rs
@trading/src/events.rs
@strategy-vault/src/contract.rs
@factory/src/lib.rs
@price-verifier/src/lib.rs
@price-verifier/src/pyth.rs
@governance/src/lib.rs
@treasury/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rustdoc and WHY annotations to the trading contract (highest audit value)</name>
  <files>
    trading/src/contract.rs,
    trading/src/interface.rs,
    trading/src/types.rs,
    trading/src/trading/rates.rs,
    trading/src/trading/market.rs,
    trading/src/trading/position.rs,
    trading/src/trading/adl.rs,
    trading/src/trading/execute.rs,
    trading/src/trading/actions.rs,
    trading/src/trading/config.rs,
    trading/src/validation.rs,
    trading/src/errors.rs,
    trading/src/constants.rs,
    trading/src/storage.rs,
    trading/src/events.rs
  </files>
  <read_first>
    trading/src/contract.rs,
    trading/src/interface.rs,
    trading/src/types.rs,
    trading/src/trading/rates.rs,
    trading/src/trading/market.rs,
    trading/src/trading/position.rs,
    trading/src/trading/adl.rs,
    trading/src/trading/execute.rs,
    trading/src/trading/actions.rs,
    trading/src/trading/config.rs,
    trading/src/validation.rs,
    trading/src/errors.rs,
    trading/src/constants.rs,
    trading/src/storage.rs,
    trading/src/events.rs,
    docs/audit/PROTOCOL-SPEC.md
  </read_first>
  <action>
Add rustdoc comments and inline annotations per D-12 requirements. This is DOCUMENTATION-ONLY -- no functional code modifications. Code freeze is in effect.

**Prioritized order of documentation (addresses review concern about scope):**

PRIORITY 1 -- Public contract entry points (trading/src/contract.rs + trading/src/interface.rs):
Full rustdoc format:
```rust
/// Brief description.
///
/// # Parameters
/// - `param` - Description (units: SCALAR_7)
///
/// # Panics
/// - `TradingError::Variant` (code) - when condition
///
/// See: Protocol Spec Section X
```
Apply to: __constructor, open_market, close_position, place_limit, cancel_limit, modify_collateral, set_triggers, execute, apply_funding, set_config, set_market, del_market, set_status, update_status
Simple getters (get_position, get_config, etc.): one-line `/// Returns the X.` only

PRIORITY 2 -- Critical internal math functions (highest audit value):
- `trading/src/trading/rates.rs`: `calc_funding_rate()`, `calc_borrowing_rate()` -- full formula in rustdoc, WHY annotations on: peer-to-peer design, util^5 exponent choice, dominant-side-only rationale
- `trading/src/trading/market.rs`: `Market::open()`, `Market::close()`, `MarketData::accrue()`, `Market::load()` -- parameter units, fee calculation steps, WHY: auto-accrue on load, payout clamped to 0
- `trading/src/trading/position.rs`: `Position::settle()`, `Position::validate()`, `require_closable()`, `require_liquidatable()` -- index-based settlement formula, WHY: MIN_OPEN_TIME prevents arbitrage, liquidation uses publish_time guard instead of MIN_OPEN_TIME

PRIORITY 3 -- Types and config (important for auditor understanding):
- `trading/src/types.rs`: Rustdoc on TradingConfig, MarketConfig, MarketData, Position struct fields explaining units (SCALAR_7, SCALAR_18, token_decimals) and purpose
- `trading/src/constants.rs`: WHY annotations on SCALAR_7 vs SCALAR_18 choice (precision over long accrual), MIN_OPEN_TIME (30s anti-arbitrage), UTIL_ONICE (95%), UTIL_ACTIVE (90%)
- `trading/src/errors.rs`: Brief one-line description of when each error is triggered
- `trading/src/validation.rs`: WHY annotations on validation bounds (why margin > liq_fee, why min_col > 0)

PRIORITY 4 -- ADL and execute (complex logic):
- `trading/src/trading/adl.rs`: ADL threshold function, factor calculation, WHY: entry-weighted average for fair distribution, UTIL_ONICE/UTIL_ACTIVE hysteresis
- `trading/src/trading/execute.rs`: execute() batch pattern, WHY: single tx for multiple keeper actions
- `trading/src/trading/actions.rs`: action orchestration for open/close/modify

PRIORITY 5 -- Storage and events (lower value, document if budget allows):
- `trading/src/storage.rs`: WHY on TTL durations (14/21 for positions, 45/52 for markets)
- `trading/src/events.rs`: one-line description of each event

**Inline WHY annotations (DOC-04) -- add ONLY where intent is non-obvious:**
1. SCALAR_7 vs SCALAR_18 usage -- why rates use SCALAR_7 but indices use SCALAR_18
2. MIN_OPEN_TIME = 30 -- prevents same-block open+close price arbitrage
3. Rounding direction in each fixed_mul/fixed_div call -- why ceil for fees, floor for PnL
4. Dominant side detection -- why only dominant side pays borrowing
5. Entry-weighted tracking for ADL -- why entry_wt matters
6. Price_scalar not stored -- why computed per verification
7. Collateral deduction order in Market::open()

**Doc review checklist per function:**
- [ ] Units specified for all parameters
- [ ] Auth model mentioned (who can call)
- [ ] Failure modes listed (which errors can panic)
- [ ] Rationale included ONLY where non-obvious

**CRITICAL: Do not change ANY functional code.** Only add comments (`///` and `//`). Verify with `cargo test --all` that all tests still pass.
  </action>
  <verify>
    <automated>cd /home/robin/Zenith/Zenex/zenex-contracts && cargo test --all --tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - trading/src/interface.rs has `///` rustdoc on every function in the Trading trait
    - trading/src/trading/rates.rs contains `/// ` and `// WHY:` annotations on calc_funding_rate and calc_borrowing_rate
    - trading/src/trading/market.rs contains rustdoc on `open`, `close`, `accrue` with parameter units
    - trading/src/trading/position.rs contains rustdoc on `settle`, `validate`, `require_closable`, `require_liquidatable`
    - trading/src/types.rs has rustdoc on TradingConfig, MarketConfig, MarketData, Position struct fields
    - trading/src/constants.rs contains `// WHY:` on SCALAR_7, SCALAR_18, MIN_OPEN_TIME
    - trading/src/validation.rs contains `// WHY:` on validation bounds
    - `cargo test --all --tests` passes (no functional changes)
    - `cargo clippy --all -- -D warnings` passes
  </acceptance_criteria>
  <done>Trading contract has comprehensive audit-focused rustdoc on all public functions and critical internal functions, plus inline WHY annotations on non-obvious design decisions. No functional code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add rustdoc comments to vault, factory, price-verifier, governance, and treasury contracts</name>
  <files>
    strategy-vault/src/contract.rs,
    factory/src/lib.rs,
    price-verifier/src/lib.rs,
    price-verifier/src/pyth.rs,
    governance/src/lib.rs,
    treasury/src/lib.rs
  </files>
  <read_first>
    strategy-vault/src/contract.rs,
    factory/src/lib.rs,
    price-verifier/src/lib.rs,
    price-verifier/src/pyth.rs,
    governance/src/lib.rs,
    governance/src/storage.rs,
    governance/src/errors.rs,
    governance/src/events.rs,
    treasury/src/lib.rs,
    docs/audit/PROTOCOL-SPEC.md
  </read_first>
  <action>
Add rustdoc comments to public functions in the remaining 5 contracts. Documentation-only -- no functional changes.

**Prioritized by audit value:**

**strategy-vault/src/contract.rs (high value -- vault security):**
- `__constructor()` -- params: name, symbol, asset, decimals_offset, strategy, lock_time. WHY: decimals_offset mitigates vault inflation attacks (per T-ELEV-11)
- `deposit()` / `mint()` -- ERC-4626 deposit, param units (assets in token_decimals, shares in vault_decimals)
- `withdraw()` / `redeem()` -- lock_time enforcement, WHY: prevents flash-deposit-withdraw
- `strategy_withdraw()` -- WHY: only trading contract can withdraw (strategy == stored_strategy)
- `strategy_deposit()` -- WHY: trading returns vault funds after profitable close
- Simple getters (total_assets, etc.): one-line description

**factory/src/lib.rs (high value -- deployment security):**
- `__constructor()` -- FactoryInitMeta contents
- `deploy()` -- WHY: deterministic address precomputation resolves circular dependency. Salt prevents frontrunning. Document deployment order (vault first, trading second).

**price-verifier/src/lib.rs + pyth.rs (high value -- oracle security):**
- `verify_price()` / `verify_prices()` -- returns PriceData, panics on invalid/stale/wrong signer
- `verify_and_extract()` -- WHY: verifies Ed25519 signature before parsing (defense in depth). Document Pyth Lazer binary format in module-level doc comment.
- `check_staleness()` -- WHY: uses abs_diff for clock skew tolerance
- Admin functions: update_trusted_signer, update_max_confidence_bps, update_max_staleness

**governance/src/lib.rs (medium value -- timelock security):**
- `queue()` -- WHY: generic invoke_contract pattern for flexibility
- `execute()` -- WHY: CEI pattern (removes queue entry BEFORE external call)
- `cancel()` -- owner-only
- `set_status()` -- WHY: bypasses delay for emergency halt
- `set_delay()` / `apply_delay()` -- WHY: delay changes are themselves timelocked via PendingDelay

**treasury/src/lib.rs (lower value -- simple contract):**
- `get_fee()` -- WHY: fixed_mul_floor rounds down (conservative for protocol)
- `set_rate()` -- bounds check documentation
- `withdraw()` -- owner transfers fees
- One-line descriptions for simple getters

**CRITICAL: Documentation only. No functional changes. Verify all tests pass.**
  </action>
  <verify>
    <automated>cd /home/robin/Zenith/Zenex/zenex-contracts && cargo test --all --tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - strategy-vault/src/contract.rs has `///` rustdoc on deposit, withdraw, strategy_withdraw, __constructor
    - factory/src/lib.rs has `///` rustdoc on deploy and __constructor with deterministic address explanation
    - price-verifier/src/lib.rs has `///` rustdoc on verify_price, verify_prices, and admin functions
    - price-verifier/src/pyth.rs has `///` rustdoc on verify_and_extract with Pyth Lazer format documentation
    - governance/src/lib.rs has `///` rustdoc on queue, execute, cancel, set_status, set_delay, apply_delay
    - treasury/src/lib.rs has `///` rustdoc on get_fee, set_rate, withdraw
    - `cargo test --all --tests` passes
    - `cargo clippy --all -- -D warnings` passes
  </acceptance_criteria>
  <done>All 6 contracts have audit-focused rustdoc comments on public functions and WHY annotations on non-obvious design decisions. Prioritized by audit value. No functional code changes.</done>
</task>

</tasks>

<verification>
1. `cargo test --all --tests` passes (documentation-only changes)
2. `cargo clippy --all -- -D warnings` passes
3. All public entry point functions across 6 contracts have rustdoc comments
4. WHY annotations exist on critical non-obvious code
</verification>

<success_criteria>
- Public entry points in all 6 contracts: rustdoc with description, params (units), panics
- Critical internal math functions: rustdoc with formulas
- Non-obvious code: inline WHY annotations
- Trivial getters: one-line description only (not verbose)
- Zero functional code changes (code freeze maintained)
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-tests-and-documentation/03-07-SUMMARY.md`
</output>
