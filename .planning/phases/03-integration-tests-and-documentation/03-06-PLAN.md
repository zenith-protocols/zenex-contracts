---
phase: 03-integration-tests-and-documentation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/audit/PROTOCOL-SPEC.md
  - docs/dev/ARCHITECTURE.md
  - docs/dev/DEPLOYMENT.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-05]

must_haves:
  truths:
    - "An auditor can read the protocol spec and independently verify the fee system math against the implementation"
    - "Architecture documentation with Mermaid diagrams shows all contract interactions, data flow, and trust boundaries"
    - "docs/ folder is organized by audience: docs/audit/ for auditors and docs/dev/ for developers"
  artifacts:
    - path: "docs/audit/PROTOCOL-SPEC.md"
      provides: "Complete protocol specification with fee formulas, position lifecycle, ADL mechanism, decimal system, and known trust assumptions"
      contains: "r_base"
    - path: "docs/dev/ARCHITECTURE.md"
      provides: "Architecture documentation with Mermaid component diagrams, data flow, and deployment overview"
      contains: "mermaid"
    - path: "docs/dev/DEPLOYMENT.md"
      provides: "Factory deployment flow documentation"
      contains: "deploy_v2"
  key_links:
    - from: "docs/audit/PROTOCOL-SPEC.md"
      to: "trading/src/trading/rates.rs"
      via: "Fee formulas match code implementation"
      pattern: "r_base.*r_var.*util"
    - from: "docs/dev/ARCHITECTURE.md"
      to: "trading/src/contract.rs"
      via: "Component diagram references actual contract entry points"
      pattern: "Trading|Vault|Factory|PriceVerifier"
---

<objective>
Create the protocol specification, architecture documentation, and docs folder structure for auditors and developers.

Purpose: DOC-01 requires a protocol spec with full math formulas. DOC-02 requires architecture documentation with component diagrams. DOC-05 requires the docs/ folder organized by audience. This is independent of testing and can run in Wave 1 in parallel with test infrastructure setup.

Output: docs/audit/PROTOCOL-SPEC.md, docs/dev/ARCHITECTURE.md, docs/dev/DEPLOYMENT.md.

Review feedback addressed:
- MEDIUM: Removed min_lines targets; replaced with required sections
- MEDIUM: Eliminated docs/dev/FEE-SYSTEM.md to avoid duplication with PROTOCOL-SPEC.md. Fee system implementation details folded into ARCHITECTURE.md under a "Fee Implementation Notes" section (addresses documentation duplication concern)
- MEDIUM: Added "Known Trust Assumptions" section to protocol spec (oracle signer trust, keeper liveness, admin/timelock trust)
- MEDIUM: Added "How to Audit This Codebase" section linking spec, threat model, matrix, and tests
- MEDIUM: Threat model move includes reference update grep
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-integration-tests-and-documentation/03-CONTEXT.md
@.planning/phases/03-integration-tests-and-documentation/03-RESEARCH.md

@trading/src/contract.rs
@trading/src/interface.rs
@trading/src/types.rs
@trading/src/constants.rs
@trading/src/trading/rates.rs
@trading/src/trading/market.rs
@trading/src/trading/position.rs
@trading/src/trading/adl.rs
@trading/src/trading/execute.rs
@trading/src/trading/actions.rs
@trading/src/trading/config.rs
@trading/src/errors.rs
@trading/src/validation.rs
@trading/src/storage.rs
@trading/src/events.rs
@strategy-vault/src/contract.rs
@factory/src/lib.rs
@price-verifier/src/lib.rs
@price-verifier/src/pyth.rs
@governance/src/lib.rs
@treasury/src/lib.rs
@docs/THREAT-MODEL.md

@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create protocol specification for auditors (DOC-01)</name>
  <files>docs/audit/PROTOCOL-SPEC.md</files>
  <read_first>
    trading/src/trading/rates.rs,
    trading/src/trading/market.rs,
    trading/src/trading/position.rs,
    trading/src/trading/adl.rs,
    trading/src/trading/execute.rs,
    trading/src/trading/actions.rs,
    trading/src/types.rs,
    trading/src/constants.rs,
    trading/src/errors.rs,
    trading/src/validation.rs,
    strategy-vault/src/contract.rs,
    price-verifier/src/pyth.rs,
    treasury/src/lib.rs
  </read_first>
  <action>
Create `docs/audit/` directory if it doesn't exist, then create `docs/audit/PROTOCOL-SPEC.md`.

This document must enable an auditor to independently verify the implementation matches the spec (per D-10). Include full math formulas with variable definitions.

**Required sections (replaces line-count targets):**

1. **Overview** -- Perpetual futures on Stellar/Soroban, leveraged positions, auto-deleveraging, ERC-4626 vault.

2. **How to Audit This Codebase** (addresses review suggestion) -- A short guide linking:
   - This protocol spec (for math verification)
   - docs/audit/THREAT-MODEL.md (for threat analysis)
   - docs/audit/THREAT-TEST-MATRIX.md (for test traceability)
   - test-suites/tests/ (for running tests)
   - Suggested audit order: spec -> threat model -> run tests -> review code

3. **Decimal System** -- SCALAR_7, SCALAR_18, price_scalar, naming conventions, fixed-point operations with rounding direction explanations.

4. **Position Lifecycle** -- Open (market order, limit order), close, modify collateral, triggers (TP/SL), keeper execution. For each: step-by-step flow with cross-contract calls.

5. **Fee System** -- Base fee, impact fee (quadratic), funding fee (peer-to-peer, zero protocol take), borrowing fee (dominant side only, util^5 curve), protocol fee (treasury rate). EXACT FORMULAS copied from the code implementation. For each formula:
   - Mathematical formula with variable definitions
   - Which file/function implements it
   - Rounding direction and why

6. **Index-Based Accrual** -- How fund_idx, borr_idx, adl_idx work. Position snapshots at fill time. Diff-based settlement.

7. **Settlement Math** -- PnL calculation, equity formula, payout clamping.

8. **Auto-Deleveraging (ADL)** -- UTIL_ONICE (95%), UTIL_ACTIVE (90%) hysteresis, ADL factor calculation, entry-weighted average, adl_idx mechanics.

9. **Vault (ERC-4626)** -- Deposit/withdraw, strategy_withdraw (trading only), lock_time, share price, decimals_offset inflation attack mitigation.

10. **Circuit Breaker** -- Status transitions (Active/OnIce/Frozen), what operations are allowed/blocked in each state, hysteresis logic.

11. **Configuration Parameters** -- Table of ALL TradingConfig and MarketConfig fields with: name, unit (SCALAR_7/SCALAR_18/raw), valid range (from validation.rs), purpose, default value.

12. **Known Trust Assumptions** (addresses review suggestion) -- What the protocol trusts and cannot verify:
    - Oracle signer: trusted to provide accurate prices (T-SPOOF-03 mitigation: signature verification)
    - Keeper liveness: relied upon for liquidation, TP/SL execution (T-DOS-02 documented risk)
    - Admin/timelock: trusted for configuration (T-ELEV-05 mitigation: timelock delay)
    - Out-of-scope services: relayer, backend, indexer

Read the actual implementation files (rates.rs, market.rs, position.rs, adl.rs) CAREFULLY to extract the exact formulas. Do NOT approximate or summarize -- auditors need exact formulas that match the code. For each formula, include the source file where the implementation lives.
  </action>
  <verify>
    <automated>cd /home/robin/Zenith/Zenex/zenex-contracts && test -f docs/audit/PROTOCOL-SPEC.md && grep -c "SCALAR_7\|SCALAR_18\|r_base\|r_var\|fund_idx\|borr_idx\|adl_idx\|UTIL_ONICE" docs/audit/PROTOCOL-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/audit/PROTOCOL-SPEC.md exists
    - File contains "How to Audit This Codebase" section with links to threat model and test matrix
    - File contains "Decimal System" section with SCALAR_7, SCALAR_18, price_scalar definitions
    - File contains "Position Lifecycle" section with open/close/modify flows
    - File contains "Fee System" section with exact funding rate formula `r_funding * |L - S| / (L + S)`
    - File contains exact borrowing curve formula `r_base * (1 + r_var * util^5)`
    - File contains "Settlement Math" section with PnL and equity calculations
    - File contains "Auto-Deleveraging" section with ADL threshold and mechanism
    - File contains "Configuration Parameters" section with table of TradingConfig and MarketConfig fields
    - File contains "Known Trust Assumptions" section listing oracle, keeper, admin trust
    - All formulas reference their source implementation file
  </acceptance_criteria>
  <done>Protocol specification covers fee system, position lifecycle, ADL mechanism, decimal system, and trust assumptions with full math formulas that an auditor can independently verify against the implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create architecture documentation, deployment docs, and move threat model (DOC-02, DOC-05)</name>
  <files>
    docs/dev/ARCHITECTURE.md,
    docs/dev/DEPLOYMENT.md
  </files>
  <read_first>
    .planning/codebase/ARCHITECTURE.md,
    .planning/codebase/INTEGRATIONS.md,
    trading/src/contract.rs,
    trading/src/interface.rs,
    factory/src/lib.rs,
    strategy-vault/src/contract.rs,
    price-verifier/src/lib.rs,
    governance/src/lib.rs,
    treasury/src/lib.rs,
    trading/src/storage.rs,
    trading/src/events.rs,
    docs/THREAT-MODEL.md
  </read_first>
  <action>
Create `docs/dev/` directory and documentation files. Move THREAT-MODEL.md to docs/audit/ (per D-11).

**Step 1: Move THREAT-MODEL.md to docs/audit/**
```bash
mkdir -p docs/audit docs/dev
mv docs/THREAT-MODEL.md docs/audit/THREAT-MODEL.md
```

Then grep for references and update them (addresses review concern about broken references):
```bash
grep -r "docs/THREAT-MODEL.md" .planning/ docs/ CLAUDE.md 2>/dev/null
```
Update any found references to `docs/audit/THREAT-MODEL.md`.

**Step 2: Create docs/dev/ARCHITECTURE.md** (per D-13 using Mermaid diagrams):

Required sections:
- **System Overview**: Mermaid flowchart showing all 6 contracts and their relationships
  - Show: User, Keeper, LP, Governance Admin as external actors
  - Show: Trading, Vault, PriceVerifier, Treasury, Timelock, Factory as contracts
  - Show: Pyth Oracle as external dependency
  - Label edges with function names

- **Open Position Flow**: Mermaid sequence diagram (User -> Trading -> PV -> Vault -> Treasury)

- **Close Position Flow**: Mermaid sequence diagram

- **ADL Flow**: Mermaid sequence diagram (update_status trigger)

- **Data Model**: Key types (TradingConfig, MarketConfig, MarketData, Position) with field descriptions

- **Storage Architecture**:
  - Instance storage (hot data): config, status, addresses. TTL: 30/31 day
  - Persistent storage: MarketConfig, MarketData, Position. TTL varies by type
  - Position TTL: 14/21 day (short-lived)
  - Market TTL: 45/52 day (medium-lived)

- **Layer Architecture**: Table mapping layers to files (Contract layer, Business logic, Domain models, Storage, Dependencies, Types, Validation, Events)

- **Contract Entry Points**: Table per contract listing all entry points, parameters, access control level

- **Fee Implementation Notes** (consolidation -- replaces separate FEE-SYSTEM.md to avoid duplication):
  - Where each fee type is computed (file:line reference)
  - SCALAR_7 vs SCALAR_18 decision points
  - How indices work (fund_idx, borr_idx, adl_idx)
  - Rounding direction conventions: ceil for fees (collect more), floor for PnL (pay less)
  - Dominant side detection logic

**Step 3: Create docs/dev/DEPLOYMENT.md:**

Document the factory deployment flow:
1. Prerequisites: upload trading + vault WASM hashes
2. Create FactoryInitMeta (trading_hash, vault_hash, treasury)
3. Call factory.deploy(admin, salt, token, price_verifier, config, vault_name, vault_symbol, decimals_offset, lock_time)
4. Internal steps:
   a. compute_salts: derives deterministic vault_salt and trading_salt from user salt
   b. deployed_address: precomputes both addresses before either is deployed
   c. Deploy vault first (no cross-call dependency at construction)
   d. Deploy trading second (receives vault address, stores it)
   e. Result: trading.get_vault() == vault_address, vault.strategy == trading_address
5. Circular dependency resolution explanation: why precomputed addresses are necessary
6. Salt-based deployment: prevents frontrunning, enables deterministic addressing
7. decimals_offset: mitigates vault inflation attacks (per T-ELEV-11)

Include the actual deploy function signature and FactoryInitMeta struct definition.
  </action>
  <verify>
    <automated>cd /home/robin/Zenith/Zenex/zenex-contracts && test -f docs/dev/ARCHITECTURE.md && test -f docs/dev/DEPLOYMENT.md && test -f docs/audit/THREAT-MODEL.md && ! test -f docs/THREAT-MODEL.md && echo "PASS: docs structure correct"</automated>
  </verify>
  <acceptance_criteria>
    - docs/dev/ARCHITECTURE.md exists with at least 2 Mermaid diagram blocks (```mermaid)
    - docs/dev/ARCHITECTURE.md contains system overview diagram with Trading, Vault, Factory, PriceVerifier
    - docs/dev/ARCHITECTURE.md contains "Fee Implementation Notes" section with SCALAR_7, fund_idx, borr_idx
    - docs/dev/ARCHITECTURE.md contains "Storage Architecture" section with TTL values
    - docs/dev/DEPLOYMENT.md exists and contains "deploy_v2" and "deterministic" and "FactoryInitMeta" and "decimals_offset"
    - docs/audit/THREAT-MODEL.md exists (moved from docs/THREAT-MODEL.md)
    - docs/THREAT-MODEL.md no longer exists at old location
    - No stale references to `docs/THREAT-MODEL.md` remain in .planning/ files (updated to docs/audit/)
  </acceptance_criteria>
  <done>Architecture documentation with Mermaid diagrams covers all contract interactions, data flow, storage, fees, and deployment. docs/ folder organized by audience: audit/ (threat model, protocol spec) and dev/ (architecture, deployment). No duplicate fee system document.</done>
</task>

</tasks>

<verification>
1. docs/audit/PROTOCOL-SPEC.md exists with full fee formulas and trust assumptions
2. docs/dev/ARCHITECTURE.md has Mermaid diagrams and fee implementation notes
3. docs/dev/DEPLOYMENT.md documents factory flow
4. docs/audit/THREAT-MODEL.md exists (moved from old location)
5. docs/ has audit/ and dev/ subdirectories
6. No stale docs/THREAT-MODEL.md references
</verification>

<success_criteria>
- Protocol spec enables independent formula verification by auditors
- Architecture docs with Mermaid diagrams show complete contract interaction topology
- Fee implementation notes consolidated in architecture doc (no duplication with protocol spec)
- docs/ folder organized by audience per D-11
- Known trust assumptions documented for auditors
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-tests-and-documentation/03-06-SUMMARY.md`
</output>
