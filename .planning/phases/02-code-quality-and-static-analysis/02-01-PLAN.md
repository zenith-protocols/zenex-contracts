---
phase: 02-code-quality-and-static-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - timelock/Cargo.toml
  - timelock/src/lib.rs
  - timelock/src/storage.rs
  - timelock/src/errors.rs
  - Cargo.toml
autonomous: true
requirements: [QUAL-08, QUAL-09]
must_haves:
  truths:
    - "A generic timelock contract exists that can forward any call to any target contract after a configurable delay"
    - "Emergency set_status calls bypass the timelock delay"
    - "Queued calls auto-expire via temporary storage"
    - "The timelock imports zero types from trading, vault, or price-verifier"
    - "The timelock has unit tests covering queue, execute, cancel, bypass, and error paths"
  artifacts:
    - path: "timelock/src/lib.rs"
      provides: "Generic timelock contract with queue/execute/cancel/set_status"
      contains: "invoke_contract"
    - path: "timelock/src/storage.rs"
      provides: "Storage keys, getters/setters, TTL management"
      contains: "TimelockKey"
    - path: "timelock/src/errors.rs"
      provides: "TimelockError enum"
      contains: "TimelockError"
    - path: "timelock/Cargo.toml"
      provides: "Crate definition with no trading dependency"
    - path: "Cargo.toml"
      provides: "Workspace includes timelock, excludes governance"
  key_links:
    - from: "timelock/src/lib.rs"
      to: "any target contract"
      via: "env.invoke_contract()"
      pattern: "invoke_contract.*target.*fn_name.*args"
    - from: "timelock/src/lib.rs"
      to: "timelock/src/storage.rs"
      via: "storage getters/setters"
      pattern: "storage::"
---

<objective>
Create a generic timelock contract that replaces the trading-specific governance contract (per D-01). The timelock uses env.invoke_contract() to forward any queued call to any target contract after a configurable delay (per D-02), with an instant bypass for set_status only (per D-03). Queued calls use temporary storage (per D-04). The timelock is included in audit scope with tests (per D-05).

Purpose: The current governance contract (223 lines) imports TradingConfig and MarketConfig, creating tight coupling. The generic timelock eliminates this coupling while supporting multi-target governance (trading, vault, price-verifier) through a single contract.

Output: New timelock/ crate replacing governance/, with unit tests covering all paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-quality-and-static-analysis/02-CONTEXT.md
@.planning/phases/02-code-quality-and-static-analysis/02-RESEARCH.md

<interfaces>
<!-- Existing governance contract being replaced (reference for pattern) -->
From governance/src/lib.rs:
```rust
// Queue/execute/cancel pattern, temporary storage with TTL, #[only_owner] macro
// Key: 223 lines, imports TradingConfig/MarketConfig/TradingClient (to be removed)
// Nonce pattern: next_market_nonce() increments instance storage counter
// TTL constants: ONE_DAY_LEDGERS = 17280, LEDGER_THRESHOLD_TEMP = 100 days, LEDGER_BUMP_TEMP = 120 days
```

From soroban_sdk (verified API):
```rust
pub fn invoke_contract<T>(&self, contract_address: &Address, func: &Symbol, args: Vec<Val>) -> T
where T: TryFromVal<Env, Val>
```

From stellar-access (reuse):
```rust
// ownable::set_owner, ownable::get_owner, #[only_owner] macro
// stellar_contract_utils::upgradeable::UpgradeableInternal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timelock crate with contract implementation and tests</name>
  <files>
    timelock/Cargo.toml,
    timelock/src/lib.rs,
    timelock/src/storage.rs,
    timelock/src/errors.rs,
    Cargo.toml
  </files>
  <read_first>
    governance/src/lib.rs,
    governance/Cargo.toml,
    Cargo.toml
  </read_first>
  <action>
1. Create `timelock/` directory with `src/lib.rs`, `src/storage.rs`, `src/errors.rs`, and `Cargo.toml`.

2. **timelock/Cargo.toml**: Model after governance/Cargo.toml but with NO dependency on the `trading` crate. Dependencies: soroban-sdk (workspace), stellar-access (workspace), stellar-contract-utils (workspace), stellar-macros (workspace). Features: testutils = ["soroban-sdk/testutils"]. crate-type = ["lib", "cdylib"]. Edition 2021. Do NOT include soroban-fixed-point-math (not needed).

3. **timelock/src/errors.rs**: Define `TimelockError` contracterror enum:
   - `NotQueued = 1` (queue entry not found or expired)
   - `NotUnlocked = 2` (timelock delay not yet passed)
   - `Unauthorized = 3` (caller is not owner)

4. **timelock/src/storage.rs**: Define storage types and helpers:
   - `TimelockKey` contracttype enum: `Delay`, `Nonce`, `Queued(u32)` (u32 is the nonce)
   - `QueuedCall` contracttype struct: `target: Address`, `fn_name: Symbol`, `args: Vec<Val>`, `unlock_time: u64`
   - TTL constants: `ONE_DAY_LEDGERS = 17280`, `LEDGER_THRESHOLD_TEMP = ONE_DAY_LEDGERS * 100`, `LEDGER_BUMP_TEMP = LEDGER_THRESHOLD_TEMP + 20 * ONE_DAY_LEDGERS` (same as governance)
   - Getters: `get_delay(e) -> u64` from instance storage using `unwrap_optimized()`, `get_queued(e, nonce) -> Option<QueuedCall>` from temporary storage (returns None if expired)
   - Setters: `set_delay(e, delay)`, `set_queued(e, nonce, queued)` with TTL extension
   - `next_nonce(e) -> u32`: increment counter in instance storage (same pattern as governance's `next_market_nonce`)
   - `remove_queued(e, nonce)`: remove from temporary storage
   - Import `unwrap::UnwrapOptimized` from soroban_sdk for storage reads (per D-12)

5. **timelock/src/lib.rs**: Implement the generic timelock contract:
   - `#![no_std]`
   - Import: soroban_sdk (contract, contractimpl, contractclient, contracttype, panic_with_error, Address, Env, Symbol, Val, Vec, IntoVal), stellar_access::ownable, stellar_contract_utils::upgradeable::UpgradeableInternal, stellar_macros::{only_owner, Upgradeable}
   - `#[derive(Upgradeable)] #[contract] pub struct TimelockContract;`
   - Define trait `Timelock` with `#[contractclient(name = "TimelockClient")]`:
     - `fn queue(e: Env, target: Address, fn_name: Symbol, args: Vec<Val>) -> u32;` (owner only)
     - `fn cancel(e: Env, nonce: u32);` (owner only)
     - `fn execute(e: Env, nonce: u32);` (permissionless -- anyone can execute after delay)
     - `fn set_status(e: Env, target: Address, status: u32);` (owner only, instant bypass per D-03)
     - `fn get_delay(e: Env) -> u64;`
     - `fn get_queued(e: Env, nonce: u32) -> QueuedCall;`
   - Constructor: `__constructor(e: Env, owner: Address, delay: u64)` -- sets owner via ownable::set_owner, stores delay in instance storage. No trading address stored (per D-02, multi-target).
   - `queue`: `#[only_owner]`, get delay, compute unlock_time = ledger().timestamp() + delay, create QueuedCall, get next nonce, store in temporary storage with TTL, return nonce.
   - `cancel`: `#[only_owner]`, check temporary storage has the key, if not panic NotQueued, remove it.
   - `execute`: Permissionless. Load QueuedCall from temporary storage (unwrap_or_else panic NotQueued). Check unlock_time <= ledger().timestamp() (else panic NotUnlocked). Call `e.invoke_contract::<Val>(&queued.target, &queued.fn_name, queued.args)`. Remove from storage.
   - `set_status`: `#[only_owner]`, instant bypass. Construct args: `vec![&e, status.into_val(&e)]`. Call `e.invoke_contract::<Val>(&target, &Symbol::new(&e, "set_status"), args)`. (Per D-03)
   - `get_delay`: Return from instance storage.
   - `get_queued`: Load from temporary storage, panic NotQueued if missing.
   - Implement `Ownable` trait: `#[contractimpl(contracttrait)] impl Ownable for TimelockContract {}`
   - Implement `UpgradeableInternal`: check operator auth and owner match (same pattern as governance).

6. **Workspace Cargo.toml**: Replace `"governance"` with `"timelock"` in the workspace members list. Keep all other members unchanged.

7. **Unit tests** in `timelock/src/lib.rs` under `#[cfg(test)] mod tests`:
   - Use `soroban_sdk::testutils::{Address as _, Ledger, LedgerInfo}` for test setup.
   - Create a mock target contract that records calls (using `#[contract]` and `#[contractimpl]`) with a `set_status` function and a generic `set_config` function that stores what it receives.
   - Test: `test_queue_and_execute` -- queue a call, advance ledger past delay, execute succeeds, verify target was called.
   - Test: `test_execute_before_delay_fails` -- queue, try execute immediately, expect NotUnlocked error.
   - Test: `test_cancel_removes_queued` -- queue, cancel, try execute, expect NotQueued error.
   - Test: `test_cancel_nonexistent_fails` -- cancel with invalid nonce, expect NotQueued error.
   - Test: `test_set_status_bypasses_delay` -- call set_status, verify target's set_status was called immediately without queuing.
   - Test: `test_queue_requires_owner` -- non-owner tries to queue, expect auth failure.
   - Test: `test_cancel_requires_owner` -- non-owner tries to cancel, expect auth failure.
   - Test: `test_nonce_increments` -- queue two calls, verify nonces are 0 and 1.
   - Test: `test_expired_queue_entry` -- (if feasible, test TTL expiration behavior).
  </action>
  <verify>
    <automated>cd /home/robin/Zenith/Zenex/zenex-contracts && cargo test -p timelock --features testutils 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'invoke_contract' timelock/src/lib.rs (generic call forwarding used)
    - grep -q 'TimelockError' timelock/src/errors.rs (error enum exists)
    - grep -q 'QueuedCall' timelock/src/storage.rs (queued call struct exists)
    - grep -q 'unwrap_optimized' timelock/src/storage.rs (per D-12)
    - grep -q 'set_status' timelock/src/lib.rs (instant bypass exists)
    - grep -q 'only_owner' timelock/src/lib.rs (owner auth enforced on queue/cancel)
    - grep -q 'timelock' Cargo.toml (workspace includes timelock)
    - ! grep -q 'governance' Cargo.toml (workspace no longer includes governance)
    - ! grep -q 'trading' timelock/Cargo.toml (no trading dependency per D-01)
    - ! grep -q 'TradingConfig' timelock/src/lib.rs (no trading types imported)
    - ! grep -q 'MarketConfig' timelock/src/lib.rs (no trading types imported)
    - cargo test -p timelock --features testutils passes with 0 failures
  </acceptance_criteria>
  <done>
    A generic timelock contract exists in timelock/ that: (1) queues any call via (target, fn_name, args) tuple, (2) executes after configurable delay via env.invoke_contract(), (3) provides instant set_status bypass, (4) imports zero types from trading/vault/price-verifier, (5) all unit tests pass including queue/execute/cancel/bypass/auth paths. Governance crate removed from workspace.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p timelock --features testutils` -- all tests pass
- `cargo build -p timelock` -- compiles without errors
- `grep -rn "TradingConfig\|MarketConfig\|TradingClient" timelock/` returns empty (no trading type imports)
- `grep -c "invoke_contract" timelock/src/lib.rs` returns >= 2 (execute + set_status)
</verification>

<success_criteria>
- Generic timelock contract compiles and all tests pass
- Zero trading-specific types imported into timelock crate
- Queue/execute/cancel/set_status all implemented with proper auth
- Governance removed from workspace members
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality-and-static-analysis/02-01-SUMMARY.md`
</output>
